module.exports = {
    content: [
        './index.html',  // Ensure index.html is included
        './src/**/*.{vue,js,ts,jsx,tsx}',  // Include all Vue and JS/TS files in src folder
    ],
    theme: {
        extend: {},  // Extend the theme if needed
    },
    plugins: [],  // Add any Tailwind plugins here
};
